# Backend Environment Variables
# ========================================================
# Copy this file to .env.local for local development
# NEVER commit .env or .env.local files to Git!

# MongoDB Atlas Configuration
# Get these from: https://cloud.mongodb.com/
MONGODB_USERNAME=your_mongodb_username_here
MONGODB_PASSWORD=your_mongodb_password_here
MONGODB_CLUSTER=cluster0.xxxxx.mongodb.net
MONGODB_DATABASE=healthhub

# JWT Secret - Generate a secure random string
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=generate_a_random_secure_string_at_least_32_characters_long

# Server Configuration
PORT=3000
NODE_ENV=development

# Production Deployment Notes:
# ========================================================
# For Render.com / Railway / Heroku:
# 1. Set these variables in your hosting dashboard (Environment section)
# 2. Use NODE_ENV=production
# 3. Generate a strong JWT_SECRET for production
# 4. Ensure MongoDB Atlas allows connections from your hosting provider's IPs
