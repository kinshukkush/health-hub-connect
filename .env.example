# This is a template file showing required environment variables
# Copy this to create your own environment files

# Frontend Environment Variables (Frontend/.env.local)
# ========================================================
VITE_API_URL=http://localhost:3000/api


# Backend Environment Variables (Backend/.env.local)
# ========================================================
# MongoDB credentials are separated for security
MONGODB_USERNAME=your_mongodb_username
MONGODB_PASSWORD=your_mongodb_password
MONGODB_CLUSTER=your_cluster.mongodb.net
MONGODB_DATABASE=healthhub

JWT_SECRET=your_super_secret_jwt_key_here
PORT=3000
NODE_ENV=development


# Production Deployment
# ========================================================
# Frontend (Vercel):
# - VITE_API_URL: Your deployed backend URL (e.g., https://your-app.onrender.com/api)

# Backend (Render/Railway/Heroku):
# - MONGODB_USERNAME: Your MongoDB Atlas username
# - MONGODB_PASSWORD: Your MongoDB Atlas password (set in hosting dashboard, NOT in Git)
# - MONGODB_CLUSTER: Your MongoDB cluster URL
# - MONGODB_DATABASE: Database name (healthhub)
# - JWT_SECRET: Strong random secret for JWT tokens
# - NODE_ENV: production
# - PORT: Auto-assigned by hosting platform (leave as 3000 locally)
