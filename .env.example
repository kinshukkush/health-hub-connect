# ========================================================
# MediTrack QA Automation Suite - Environment Variables
# ========================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to create environment-specific files:
#    - Backend/.env.local (for local development)
#    - Frontend/.env.local (for local development)
#    - playwright-tests/.env (for test execution)
# 2. Replace placeholder values with your actual credentials
# 3. NEVER commit .env files to Git!
# ========================================================


# Frontend Environment Variables (Frontend/.env.local)
# ========================================================
VITE_API_URL=http://localhost:3000/api


# Backend Environment Variables (Backend/.env.local)
# ========================================================
# MongoDB Atlas Configuration
MONGODB_USERNAME=your_mongodb_username_here
MONGODB_PASSWORD=your_mongodb_password_here
MONGODB_CLUSTER=cluster0.xxxxx.mongodb.net
MONGODB_DATABASE=healthhub

# JWT Configuration
JWT_SECRET=generate_a_random_secure_string_at_least_32_chars_long

# Server Configuration
PORT=3000
NODE_ENV=development


# Playwright Test Environment (playwright-tests/.env)
# ========================================================
BASE_URL=http://localhost:5173
API_URL=http://localhost:3000/api
TEST_USER_EMAIL=testuser@example.com
TEST_USER_PASSWORD=Test@1234


# Production Deployment Setup
# ========================================================
# Frontend (Vercel/Netlify):
#   - VITE_API_URL: https://your-backend-url.onrender.com/api
#
# Backend (Render/Railway/Heroku):
#   - Set all MONGODB_* variables in hosting dashboard
#   - JWT_SECRET: Use a cryptographically secure random string
#   - NODE_ENV: production
#   - PORT: Use hosting provider's default or set to 3000
#
# SECURITY NOTE:
#   Never hardcode production credentials in code or .env files
#   Always use hosting platform's environment variable settings
# - MONGODB_DATABASE: Database name (healthhub)
# - JWT_SECRET: Strong random secret for JWT tokens
# - NODE_ENV: production
# - PORT: Auto-assigned by hosting platform (leave as 3000 locally)
